import { TuncatePipePipe } from './tuncate-pipe.pipe';

describe('TuncatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TuncatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
