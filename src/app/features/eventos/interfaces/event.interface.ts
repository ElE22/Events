export interface Evento {
    icono: string;
    descripcion: string;
    imgURL?: string;
}

export interface Eventos {
  titulo: string;
  descripcion: string;
  fecha: string; 
  location: string;
  status: string;
}